#include <iostream>

using namespace std;

int main(){
	int i;
	
	
	for (i = 0; i < 5 ; i ++)// inicialização; condição; maneira como ira variar
	// (i += 2) == (i = i + 2)
	//i ++ == i + 1 >>> i-- == i - 1
	cout << i << endl;
	return 0;
}