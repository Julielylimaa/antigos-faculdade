//revisao prova 21/06

//materias: vetores, funçoes, structs e manipulação de strings 


//****vetores****
//sao estruturas de dados que conseguem armazenar mais de um valor
//ao mesmo tempo

//ex: int numeros[100] //vetor que salva 99 valores 
//sempre salvar N + 1 para o vetor 
//sempre precisa usar o for ou while para percorrer o vetor 
//o parametro do for deve sempre começar no zero pq a primeira posição
//de um vetor é o zero



//****struct****
//armazena um grupo de dados dentro de um dado
//estrutura:
	//struct pessoa{
		//char nome[100];
		//int idade;
	//};
	
	//int main (){
		//pessoa p1; //coloca a struct em uma variavel chamada p1
					 //pode criar um vetor de struct tbm, basta colocar um [N] na
					 //frente da indicação da variavel 
					 //ex: pessoa p1[10];
					 
			
		
		//cin.getline(p1.nome, 100);
		//cin >> p1.idade;
		
		//return 0;
	//}
	

	
	
	
//****manipulação de strings****

//utiliza biblioteca cstring
//funções da string:
	//cin.getline(x, N) = serve para pegar a linha inteira
	//strcmp(x,y) = vai comparar x é y e retornar 0 se forem iguais
		//usa o if (strcmp(x,y) == 0)
    //strcpy(x,y) = copia o que está na string y para a string x
    	//a primeira string sempre sera para onde irá ser copiado
    	//"de y para x"
 	//strlen(x) = vai pegar o tamanho da string, quantos elementos possuem em x
 	
 	
//****funções****

//o tipo da função deve ser o tipo que ela retornará


	
		
		
		

